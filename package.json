{
  "name": "msgvault-docs",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "prebuild": "mkdir -p public && curl -fsSL https://raw.githubusercontent.com/wesm/msgvault/main/scripts/install.sh -o public/install.sh.tmp && mv public/install.sh.tmp public/install.sh || rm -f public/install.sh.tmp",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.32.0",
    "astro": "^5.1.0",
    "sharp": "^0.33.0"
  }
}
